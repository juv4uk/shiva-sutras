* text=auto eol=lf
*.png binary
*.jpg binary
*.pdf binary
*.yaml text eol=lf

# WSM source files (see my-lisp repo contract). Lisp is a temporary GitHub
# Linguist display profile, not a semantic identity claim.
*.wsm linguist-language=Lisp
*.my linguist-language=Lisp
*.lisp linguist-language=Lisp
