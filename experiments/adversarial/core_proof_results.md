# Experiment 5.5b: Sufficient & Minimal Skeleton Proof

## 1. Sufficiency Test
**Result:** SAT. The 7 classes alone are NOT sufficient to force M=14.

## 2. Minimality Test (Leave-One-Out on the Core)
- Removed `aṇ`: **SAT** (M drops to 13). `aṇ` is structurally necessary.
- Removed `aṭ`: **SAT** (M drops to 13). `aṭ` is structurally necessary.
- Removed `eṅ`: **SAT** (M drops to 13). `eṅ` is structurally necessary.
- Removed `aic`: **SAT** (M drops to 13). `aic` is structurally necessary.
- Removed `yañ`: **SAT** (M drops to 13). `yañ` is structurally necessary.
- Removed `jhaṣ`: **SAT** (M drops to 13). `jhaṣ` is structurally necessary.
- Removed `chav`: **SAT** (M drops to 13). `chav` is structurally necessary.

## Conclusion
The 7 classes alone are **NOT SUFFICIENT** to force M=14. They require the background pressure of the other 35 classes to push the system to the breaking point. The complexity is **holistic**.
